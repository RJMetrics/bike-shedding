import React from 'react';

const TopBar = props => {
  return(
    <div className="topbar">
        <i className="fa fa-bar-chart fa-fw" aria-hidden="true"></i>
        <span className="topbar-title">Quarterly Indego Rides Anaylitics</span>
    </div>
  );
};

export default TopBar;
